# fibonacci-as: AssemblyScript example of fibonaci 

## Building

- Clone this repo to `fibonacci-as` and switch to that directory.
- Use `npm i` to install all dependencies.
- Use `npm run asbuild` to build a WASM binary.
